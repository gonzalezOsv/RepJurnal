# ========================================
# FITNESS TRACKER - ENVIRONMENT VARIABLES
# ========================================
# 
# ⚠️ IMPORTANT SECURITY INSTRUCTIONS:
# 
# 1. Copy this file to .env:
#    cp env.example .env
# 
# 2. Generate strong secrets using:
#    python3 -c "import secrets; print(secrets.token_hex(32))"
# 
# 3. Replace ALL placeholder values below
# 
# 4. NEVER commit .env to git (it's in .gitignore)
# 
# 5. Use different secrets for dev/staging/production
# 
# ========================================

# Flask Configuration
FLASK_ENV=development
FLASK_APP=app

# ========================================
# SECRET KEYS (CRITICAL - CHANGE THESE!)
# ========================================
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# 
# Minimum 32 characters, use the output from command above
SECRET_KEY=CHANGE_THIS_GENERATE_WITH_COMMAND_ABOVE_MIN_64_CHARS
JWT_SECRET_KEY=CHANGE_THIS_GENERATE_WITH_COMMAND_ABOVE_MIN_64_CHARS

# ========================================
# DATABASE CONFIGURATION
# ========================================
# Database name
DB_NAME=fitness_tracker

# Database user (change in production)
DB_USER=flaskuser

# Database password (CHANGE THIS! Minimum 20 characters)
# Use a password manager to generate a strong password
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_MIN_20_CHARS

# Database root password (CHANGE THIS! Minimum 24 characters)
# This is the MySQL root user password
DB_ROOT_PASSWORD=CHANGE_THIS_VERY_STRONG_ROOT_PASSWORD_MIN_24_CHARS

# Database URI (auto-constructed from above, no need to change)
SQLALCHEMY_DATABASE_URI=mysql+pymysql://${DB_USER}:${DB_PASSWORD}@db/${DB_NAME}

# ========================================
# PRODUCTION CONFIGURATION
# ========================================
# For production deployment, also set:
# 
# FLASK_ENV=production
# 
# And use a production database host:
# SQLALCHEMY_DATABASE_URI=mysql+pymysql://prod_user:prod_pass@prod_host:3306/fitness_tracker
# 
# ========================================

# ========================================
# SECURITY CHECKLIST
# ========================================
# Before deploying, ensure:
# [ ] All secrets are unique and strong (64+ characters)
# [ ] DB passwords are 20+ characters
# [ ] No default values are used
# [ ] .env file is NOT committed to git
# [ ] Different secrets for dev/staging/production
# [ ] Run: python scripts/security_check.py
# ========================================

