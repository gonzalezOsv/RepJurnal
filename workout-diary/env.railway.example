# ===================================
# RAILWAY DEPLOYMENT - ENVIRONMENT VARIABLES
# ===================================
# Copy these to Railway's "Variables" tab
# DO NOT commit actual values to GitHub!
# ===================================

# Flask Settings
FLASK_ENV=production
SECRET_KEY=generate-new-secret-key-using-secrets-token-hex-32
JWT_SECRET_KEY=generate-new-jwt-secret-key-using-secrets-token-hex-32

# Database Auto-Initialization (only for first deployment)
# Set to 'true' for first deploy, then change to 'false' after tables are created
AUTO_INIT_DB=true

# Database Connection - Railway automatically provides these:
# Your app will read Railway's MYSQL_* variables automatically!
# No need to set DB_HOST, DB_USER, etc. manually

# Railway MySQL provides these automatically (nothing to configure):
# - MYSQL_HOST
# - MYSQL_PORT  
# - MYSQL_DATABASE
# - MYSQL_USER
# - MYSQL_PASSWORD
# - MYSQL_URL / DATABASE_URL

# Your app automatically uses these Railway variables!

# Railway automatically provides PORT variable
# No need to set PORT manually

# ===================================
# HOW TO GENERATE SECRET KEYS:
# ===================================
# Run this command in Python:
# python -c "import secrets; print(secrets.token_hex(32))"
# 
# Do this TWICE (once for SECRET_KEY, once for JWT_SECRET_KEY)
# Use different values for each!
# ===================================

# ===================================
# RAILWAY DEPLOYMENT NOTES:
# ===================================
# 1. Add MySQL database in Railway dashboard
# 2. Copy these variables to Railway's "Variables" tab
# 3. Use Railway's MySQL reference variables (the ${{...}} syntax)
# 4. Generate NEW secret keys (don't use defaults!)
# 5. Set FLASK_ENV=production
# 6. Railway will auto-set PORT variable
# ===================================

